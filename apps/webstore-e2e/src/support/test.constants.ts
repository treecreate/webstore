// Various enums from the webstore app
// ToDo - extract them into a library that all projects can use
export enum LocalStorageVars {
  locale = 'locale',
  cookiesAccepted = 'cookiesAccepted',
}

export enum CookieStatus {
  accepted = 'accepted',
  rejected = 'rejected',
  undefined = 'undefined',
}

export enum LocaleType {
  dk = 'dk',
  en = 'en-US',
}
